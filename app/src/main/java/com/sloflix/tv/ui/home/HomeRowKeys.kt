package com.sloflix.tv.ui.home

/** Stable keys for synthetic home rows — translated in the UI via LocalStrings. */
object HomeRowKeys {
    const val ContinueWatching = "__continue_watching__"
    const val SearchResults = "__search_results__"
    const val Results = "__results__"
}
